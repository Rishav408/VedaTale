services:
  - type: web
    name: vedatale-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: cd backend && gunicorn --bind 0.0.0.0:$PORT backend:app
    envVars:
      - key: GOOGLE_API_KEY
        sync: false

